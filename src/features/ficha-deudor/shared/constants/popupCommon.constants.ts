import type { CSSProperties } from 'react';

export const POPUP_WIDE_MAIN_STYLE: CSSProperties = {
  overflow: 'hidden',
  display: 'flex',
  flexDirection: 'column',
};

export const POPUP_WIDE_TABLE_WRAPPER_STYLE: CSSProperties = {
  flex: 1,
  overflowX: 'auto',
  overflowY: 'hidden',
};