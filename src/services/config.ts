/**
 * Cuando estés listo para producción, cambias esto a false
 * y todo el sistema empieza a usar fetch() sin tocar los componentes
 */
export const USE_MOCK = true;

export const API_BASE = '/api';